# stable-metaswarm

Оркестратор CLI-агентов от разных вендоров. Ведёт задачу по конвейеру
тех-дизайн → граф задач → таск-планы → реализация → ревью, с циклами проверки
«А сделал — Б проверил — А поправил» (максимум три круга, дальше эскалация к
человеку) и остановкой ветки на открытых вопросах до ответа человека.

**Статус: тех-дизайн написан, кода ещё нет.** Стек и защищённые правила
findings выбраны, открытых вопросов к требованиям нет. Для пакета P1-A планы
T1.1–T1.7b написаны и прошли индивидуальное ревью, но как implementation
baseline пока не зафиксированы. Перед T1.7b владелец явно закрыл Q47/Q48: поздний finding получает положительно
помеченную строку `post_check`, а любой branch-scoped human gate атомарно
переводит ветку в `blocked`. Что решено —
`docs/metaResearches/decision.md`; как устроено — `docs/design/`; требования —
`docs/requirements/`.

## Что где лежит

| Путь | Что это |
|---|---|
| `docs/requirements/initial.md` | Исходные требования от пользователя, как он их сформулировал |
| `docs/requirements/open-questions.md` | Что в требованиях не определено: вопросы с вариантами ответа и оценкой уверенности. Ответы вписываются туда же, в строку `**Ответ:**` |
| `docs/researches/` | **Предварительные** ресёрчи ландшафта, собранные веб-агентами по README, статьям и блогам — по файлу на модель. Исходный материал, не источник фактов |
| `docs/analysis/` | Проверка отдельных утверждений ресёрчей по коду, со ссылками на строки |
| `docs/metaResearches/` | Сводные выводы: все ресёрчи переработаны вместе с проверкой по коду в `refs/`. Отсюда берётся решение, что использовать и что писать |
| `docs/design/` | Тех-дизайн: архитектура, схема SQLite с разметкой инвариантов, контракты агентов, граф задач. Отвечает на «как исполнимо», тогда как `decision.md` — на «что и почему» |
| `docs/task-plans/` | Детальные таск-планы по пакетам и единый `TEMPLATE.md`; каждый план привязан к задаче графа и точным baseline-ревизиям |
| `docs/handoff/` | Передачи между сессиями и средами по шаблону cut-off: задача, состояние, следующее действие, критерии приёмки. Читается вместо всего остального, когда подхватываешь чужую работу |
| `refs/` | Клоны анализируемых проектов. **Только чтение, не редактировать.** В `.gitignore` |
| `refs/manifest.tsv` | Что тянем и зачем |
| `refs/pinned.tsv` | Коммиты, на которых сделаны выводы |
| `scripts/sync-refs.ps1` | Разложить `refs/` по манифесту |
| `scripts/checks/` | Прогоняемые проверки утверждений дизайна: сценарий на SQLite и раннер к нему. Документ ссылается на прогон — прогон лежит здесь и воспроизводится |

## Язык

- С пользователем — по-русски, простым и доступным языком. Он разработчик, но это
  не повод усложнять.
- Документация — тоже по-русски.
- Технические термины, имена файлов, функций и флагов — в оригинальном виде.

## Git

- **Создал файл — сразу `git add`.** Не «потом, вместе с коммитом». Документы
  этого проекта существуют в одном экземпляре, и untracked файл теряется молча:
  его не видно ни в diff, ни в истории, ни у пользователя на другой машине.
- **Закончил шаг — коммит и `git push`.** Шаг — это законченная единица работы:
  документ дописан, раздел переписан, вопрос закрыт, ревью внесено. Не каждая
  правка и не «в конце сессии».
- Спрашивать разрешения на коммит и пуш не нужно — это правило и есть
  разрешение. Ветка `main`, remote `origin`.
- Сообщение коммита — по-русски, по существу: что изменилось и зачем, а не
  «update docs».

## Имена файлов в `docs/`

На Windows файловая система не различает регистр, поэтому файл с именем
`claude.md` в любом каталоге подхватывается харнессом как `CLAUDE.md` — файл
инструкций — и его содержимое попадает в контекст сессии как указание. Так уже
случилось однажды: разбор `docs/metaResearches/claude.md` с отменённым стеком
загружался как действующая инструкция, пока его не переименовали в
`pass-claude.md`. Не называй файлы `claude.md` или `agents.md`.

## Работа с ресёрчами

`docs/researches/` — это предварительный слой: каждый файл написан отдельным
веб-агентом по README, статьям и блогам, без чтения исходников. Документы
расходятся между собой и местами расходятся с кодом. Утверждение оттуда —
гипотеза, а не факт: проверяй по исходникам в `refs/` и ссылайся на файл и
строку.

Дальше идут два производных слоя:

- `docs/analysis/claims-to-verify.md` — точечная проверка отдельных claim'ов,
  от которых зависит выбор архитектуры.
- `docs/metaResearches/` — сводные картины по моделям (`pass-claude.md`, `codex.md`,
  `kimi.md`): ресёрчи + проверка по коду + ответы из `open-questions.md`.
- `docs/metaResearches/decision.md` — **рабочая выжимка решения без истории**:
  стек, границы v1, топология, модель данных, протокол ревью, инварианты,
  приёмочные сценарии, допущения и порядок работ. **Начинать отсюда** — с этим
  документом идут в реализацию.
- `docs/metaResearches/finalDecision.md` — как к решению пришли: §0 —
  зафиксированные решения владельца (отменяют §1–§6, первый проход, по оси
  рантайма), §7 — вердикты агентов, §8 — таблица принятия, §9, §10 и §17 —
  завершённые обсуждения с доводами. Нужен, когда надо понять, **почему** так, а
  не **что** решено.
- `docs/metaResearches/planesExplained.md` — то же решение простым языком:
  менее очевидные плоскости (как брать чужой код, цена beads, личность
  finding'а, мелочи вроде systemd и расположения каталога состояния) с
  примерами и честными минусами каждого варианта.

Если новая проверка кода противоречит `metaResearches`, правь `metaResearches`
и указывай, какое утверждение отменено — там уже есть раздел с поправками к
предыдущим выводам, включая наши собственные.

## Решения можно пересматривать

Выбранные инструменты, библиотеки и механизмы — не самоцель. Если при
проектировании или в коде видно, что другой путь заметно проще, — предлагай
смену и записывай, что отменено и почему. Молча следовать неудачному решению
хуже, чем его пересмотреть.

Требования — тоже не догма: **поправку к требованию предлагать разрешено**, если
она существенно упрощает реализацию. Выносить на решение с ценой вопроса, а не
обходить втихую.

Не двигается без явного разговора одно: контракт разрешения findings и правила
остановки ветки. В них смысл системы.

## Как менять схему: сначала инвариант, потом DDL

Цикл «правка → ревью → новый контрпример → правка» в блоке C-01/C-04 занял
двадцать раундов, потому что каждый раз чинился присланный пример, а не
утверждение целиком. Правило на будущее: **перед изменением схемы заполняется
блок ниже, и только потом пишется DDL.**

```
Инвариант (одной фразой, сильнее контрпримера):
Запрещённые состояния:
Источник истины:
Создающая операция и её владелец:
Транзакционная граница (с чем пишется атомарно):
Допустимые формы по role/kind/reason (таблица):
Scope-координаты (какие ID обязаны быть из одного run/campaign/round):
Разрешённые UPDATE / DELETE:
Поведение при retry и после падения:
Что держит база / что операция / что только диагностирует recovery:
Негативные контрпримеры (список до правки):
Что синхронизировать в планах и графе задач:
```

Пять правил, которые ловят большую часть ошибок этого блока:

1. **Новая durable-таблица — не локальная правка.** Она сразу тянет scope,
   lifecycle, immutability, владельца, recovery и inventory. Если нельзя одной
   строкой сказать «создаёт X в транзакции Y вместе с Z; читает W; повтор
   отсекается ключом K» — проектирование не закончено.
2. **Один негативный тест — одно нарушение.** Подготовка обязана быть валидной
   по всем прочим ограничениям, иначе тест доказывает не то, что написано в
   подписи.
3. **Mutation-проверка обязательна для каждого нового констрейнта.** Каждое
   ограничение записывается в `scripts/checks/mutations.tsv` мутационным
   случаем — строкой
   «файл → мутация → **назначенный шаг**», и
   `python3 scripts/checks/mutation-check.py scripts/checks/mutations.tsv`
   требует, чтобы после снятия ограничения покраснел именно этот шаг. Просто
   «стало красным» не годится: мутация меняет состояние базы и роняет соседей.
   Перекрывающиеся ограничения снимаются вместе через `&&`. Инструмент уже
   нашёл: два теста доказывали чужой ключ, два ключа были избыточны, а один
   сценарий вообще падал по `PRIMARY KEY` вместо проверяемого правила.
4. **Многосоставный trigger доказывается по ветвям.** Одна мутация всего
   trigger'а показывает лишь, что нужен хотя бы один дизъюнкт. Для каждого
   верхнеуровневого условия `WHEN ... OR` и каждой колонки многоколоночного
   `UPDATE OF` нужна отдельная строка манифеста и свой назначенный шаг. Это
   проверяется по умолчанию для всех trigger'ов, а не opt-in меткой. Если форма
   намеренно неразложима, допустима только явная строка
   `@mutation-granular-exempt <trigger>: <причина>`; молчаливого исключения нет.
   Сценарий input-поля обязан завершать attempt/round и одновременно менять
   ровно это поле: обычный UPDATE активной строки может быть отвергнут общим
   правилом terminal-перехода и ничего не доказать про immutable input.
5. **Запрет DELETE проверяется и через `INSERT OR REPLACE`.** В SQLite скрытый
   DELETE конфликтующей строки вызывает delete-trigger только при
   `PRAGMA recursive_triggers=ON`. Поэтому pragma обязательна с readback на
   каждом соединении, а у append-only/evidence-таблиц нужны оба adversarial-
   сценария: прямой DELETE и REPLACE по конфликтующему ключу.

Долг записывается **строкой манифеста** со статусом `todo: <причина>`, а не
комментарием: комментарий исчезает из счёта, и «64 из 64» выглядит как успех.
Инструмент печатает «доказано X / всего Y / TODO Z» и перед мутациями проверяет,
что исходный сценарий зелёный, — иначе мутация доказывает не то.

Но и «78 из 78» само по себе ничего не говорит о полноте: строка, которой в
манифесте нет, в счёт не попадает вовсе. Это второй, отдельный вид долга, и у
него свой механизм —
`python3 scripts/checks/mutation-check.py --coverage scripts/checks/mutations.tsv`
перечисляет ограничения sql-файлов без единой строки манифеста и возвращает
ненулевой код, пока такие есть. Разрыв нашёлся ровно так, как и должен был:
внешним ревью, вручную, через полгода после введения правила. Теперь
различаются три вида долга: `--coverage` — «строки нет вообще», `todo:` —
«строка есть, сценарий не изолирован», granular coverage — «строка на trigger
есть, но отдельная ветвь `WHEN` или колонка `UPDATE OF` не имеет свидетеля».

Тот же режим отделяет третий случай — **мёртвый ключ**. `UNIQUE`, куда входит
первичный ключ, не может отвергнуть ни одной строки: PK уже уникален. Такой
ключ существует ровно затем, чтобы на него ссылались составным FK; если никто
не ссылается, ему нужна не мутация, а удаление. Без этого различения при триаже
покрытия мёртвый ключ «закрывается» первой попавшейся мутацией, и мусор в схеме
получает вид доказанного.

Отдельно запускается
`python3 scripts/checks/mutation-check.py --schema-sync docs/design/db-schema.md scripts/checks`:
он сверяет design со всеми SQL-сценариями в обе стороны: полный набор и тела
`CREATE TRIGGER`, а также table-level `CHECK`/FK/`UNIQUE` из реально собранных
SQLite-схем. Это ловит ограничение, которого нет ни в одном стабе и которое
поэтому принципиально не могло попасть в обычный `--coverage`. Отсутствующий
свидетель — долг и ненулевой exit, даже если trigger-parity уже чистая;
инлайновые одноколоночные FK и родительские `UNIQUE (id, <scope>)` исключаются
по тем же явно названным причинам, что в structural coverage.

После последней schema-правки запускается и связный прогон нормативных SQL-
блоков:
`python3 scripts/checks/run-sql-check.py --design-schema docs/design/db-schema.md`.
Он сверяет заявленный inventory с `sqlite_master`, читает все views, выполняет
`foreign_key_check` и точный readback PRAGMA. Изолированные срезы и мутации он
не заменяет: связный прогон доказывает, что части собираются вместе, а срезы —
что конкретное запрещённое состояние действительно отвергается.

**Производное число пишется как разность двух машинных, а не считается в уме.**
«Добавлено N строк» — это `всего_после − всего_до` из вывода инструмента.
Ошибка в счётной фразе случалась трижды за два блока (счёт FK, счёт уточнений,
«двенадцать строк» вместо одиннадцати), и каждый раз число было получено
пересчётом глазами при доступном машинном.

Раннер прогоняет каждый шаг с ожиданием внутри `SAVEPOINT` и откатывает его при
несовпадении: без этого одна разрешённая мутацией вставка роняет соседние шаги,
и назначенный ID попадает в отчёт «за компанию».

Владельца ищем по всей цепочке — `domain action → операция → владелец
транзакции → repository → DDL → потребитель → recovery → AC в плане`. Если один
слой называет владельцем задачу, которая по своему плану чистая функция без
базы, работа останавливается до устранения противоречия.

И формулировка результата: пока не пройден отдельный adversarial-проход, писать
«исходный контрпример закрыт», а не «находка закрыта».

## Тех-дизайн

`docs/design/` — слой реализации поверх решения. Начинать с
`design/README.md`, дальше `architecture.md`.

Правило то же, что для ресёрчей, только в другую сторону: если при
проектировании или в коде выясняется, что решение чего-то не учло, — правь
`decision.md` и записывай уточнение в `design/db-schema.md` §14 «Уточнения к
decision.md». Молча разойтись документам нельзя: `decision.md` остаётся
источником «что и почему», и агент, читающий только его, не должен получить
неверную картину.
