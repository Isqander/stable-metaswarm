# Review brief: состояние тех-дизайна перед реализацией

Короткая передача независимому ревьюеру. Цель ревью — проверить не стиль
документов, а согласованность решений, инвариантов, контрактов и границ задач до
появления production-кода.

**База ревью:** актуальный `main` после `git pull origin main`; перед работой
зафиксировать `git rev-parse HEAD` в отчёте. Исторический
[`m0-vendor-cli.md`](m0-vendor-cli.md) описывает постановку уже закрытой фазы
M0 и текущим списком работ не является.

## 1. Текущий статус

| Область | Статус |
|---|---|
| Требования и вопросы владельцу | Закрыты; ответы в [`open-questions.md`](../requirements/open-questions.md) |
| Решение «что и почему» | Зафиксировано в [`decision.md`](../metaResearches/decision.md) |
| Архитектура, DDL, agent contracts | Содержательно закрыты после трёх раундов внешнего ревью |
| Vendor CLI | M0 закрыт реальными headless-запусками на Linux VPS |
| Production-код | Не начат |
| Исполняемый код в репозитории | Только [`fake-cli.py`](../../fixtures/vendor-cli/fake-cli.py) и golden fixtures для будущих тестов |
| Следующий этап | Таск-планы пакета P1-A, их индивидуальное и совместное ревью; затем реализация |

Тех-дизайн проверялся тремя формальными внешними раундами. Основные серии
исправлений: `3de3c9e`, `324d5db`, `08fea83`; follow-up по осознанной остановке
круга — `2761ee1`. M0 и последующее ревью fixtures дополнительно уточнили дизайн
в `cc8cd5f` и `8804bfd`.

Проектирование обнаружило восемь пропусков в исходном решении; внешнее ревью
схемы — шесть воспроизведённых ошибок первой редакции. Среди них: неверная
граница периода severity, неработающее понижение человеком, пропуск `NULL` через
SQLite `CHECK`, возможность назначить автора ревьюером своей ревизии и неполная
защита `author_revision`.

## 2. Что спроектировано

- один Python-сервис и отдельный CLI-процесс через UDS;
- SQLite как источник истины, один writer, явные транзакционные границы и
  append-only `run_event`;
- чистый домен без I/O и отдельный слой внешних эффектов;
- durable recovery процессов, git checkpoint, артефактов и outbox;
- протокол findings с постоянными ID, reconciliation, severity, капом
  `3 правки / 4 проверки` и эскалацией человеку;
- профили vendor CLI как данные, фактическая пара provider+model, read-only
  ревьюер и drift check;
- три флоу v1: ревью коммита, фича в существующий проект, новый проект;
- проверяемая декомпозиция до первого real-CLI флоу и крупные блоки остатка MVP.

Оценка из дизайна: фаза 1 — 56.5 рабочих дня плюс буфер 3 дня на первый реальный
прогон; до MVP — около 90–95 рабочих дней. Это ориентир до появления измеренной
скорости, пересмотр предусмотрен после M3.

## 3. Пакеты таск-планов

Границы и процесс зафиксированы в [`task-plans.md`](../design/task-plans.md)
§3.1.

| Пакет | Состав | Рубеж перед следующим пакетом |
|---|---|---|
| P1-A · review core | T1.1–T1.7, T1.7b | M1 + M1b: чистая логика и вертикальный срез на SQLite |
| P1-B · process boundary | T1.8, T1.9, T1.10a, T1.11 | CLI runner, liveness, recovery попыток, readiness |
| P1-C · durable effects | T1.12, T1.13, T1.14, T1.16, T1.10b | M2: полное восстановление состояния и внешних эффектов |
| P1-D · orchestration | T1.17–T1.22a, кроме T1.15 | M3: флоу 1 проходит на fake CLI |
| P1-E · real environment | T1.23; независимо T1.15 | M4: стабильные real-CLI прогоны; Telegram не блокирует |

Внутри пакета: написать и проверить каждый план отдельно, затем проверить весь
пакет на стыках двумя последовательными кворумами, зафиксировать baseline и
только после этого реализовывать. Следующий пакет планируется после фактического
рубежа предыдущего. Фазы 2–3 детализируются после M4.

Уже при фиксации пакетов обнаружен пропущенный edge графа: T1.11 мог формально
начаться до T1.10a, хотя readiness обязана идти после recovery и orphan kill.
Зависимость исправлена на `T1.10a → T1.11`; ревьюеру стоит искать аналогичные
расхождения между prose-контрактом и стрелками графа.

## 4. Маршрут чтения

| Файл | Что проверять |
|---|---|
| [`decision.md`](../metaResearches/decision.md) | Источник решений: границы v1, модель данных, findings, роли, флоу, инварианты и приёмочные сценарии |
| [`design/README.md`](../design/README.md) | Навигация, статус и краткая история найденных ошибок |
| [`architecture.md`](../design/architecture.md) | Границы компонентов, process model, state machines, recovery, effects, безопасность и тестирование |
| [`db-schema.md`](../design/db-schema.md) | Полный DDL, представления, транзакционные срезы, разметка 29 инвариантов; обязательно §14 с уточнениями к решению |
| [`agent-contracts.md`](../design/agent-contracts.md) | Входы и ответы восьми ролей, выдача ID и полный чек-лист валидации |
| [`vendor-cli-contracts.md`](../design/vendor-cli-contracts.md) | Проверенные argv, parsing/completion, модели, resume, read-only, SIGTERM и ограничения M0 |
| [`task-plans.md`](../design/task-plans.md) | Граф, критерии готовности, зависимости, пакеты P1-A–P1-E и оценки |
| [`fixtures/vendor-cli/README.md`](../../fixtures/vendor-cli/README.md) | Как устроены golden fixtures и режимы отказов fake CLI |
| [`open-questions.md`](../requirements/open-questions.md) | Ответы владельца, если утверждение дизайна выглядит как неоговорённое требование |

[`docs/researches/`](../researches/) — предварительный материал, не источник
фактов. [`refs/`](../../refs/) — pinned-клоны для проверки утверждений, только
чтение.

## 5. Что ещё не доказано

1. Полная DDL ни разу не выполнялась как единая миграция; отдельно исполнялись
   несущие конструкции и негативные вставки. Это первая содержательная проверка
   T1.2–T1.3.
2. Agent contracts не проходили через настоящий рантайм и реальные циклы
   повторов; пока это проверенный рассуждением контракт.
3. Recovery через `METASWARM_ATTEMPT_ID` в `/proc/*/environ` проверен не на
   WSL2, а M0 выполнялся на обычном Linux VPS.
4. `cursor-agent` на VPS отсутствует; профиль желателен, но не блокирует v1.
5. Vendor fixtures — один smoke-снимок конкретных версий, поэтому версии входят
   в drift check, а real-vendor smoke tests остаются opt-in.
6. Фазы 2–3 намеренно описаны крупно и не готовы к реализации без нового цикла
   планирования после M4.

## 6. Просьба к ревьюеру

Проверить прежде всего:

1. нет ли противоречий между `decision.md`, архитектурой, DDL и agent contracts;
2. каждый ли из 29 инвариантов действительно удерживается указанным слоем и
   атомарной транзакцией;
3. не теряется ли finding и не сбрасывается ли severity при reopen,
   `accepted_reason`, human override и переходе между кампаниями;
4. корректны ли четыре crash-границы и порядок orphan kill → recovery audit →
   readiness;
5. не может ли автор или уже экспонированная фактическая модель стать
   запрещённым ревьюером;
6. не смешаны ли domain decision, внешний effect и его recovery;
7. замкнуты ли пакеты P1-A–P1-E: нет ли скрытой зависимости вперёд, дублирования
   ответственности или результата, который некому потребить;
8. достижимы ли критерии готовности задач и приёмочные сценарии §14 на том
   рубеже, где они заявлены.

Ожидаемый отчёт: findings по severity с точным файлом и строкой, нарушенным
инвариантом или сценарием, объяснением наблюдаемого отказа и минимальной правкой.
Отдельно перечислить вопросы, где без решения владельца есть два существенно
разных корректных пути. Косметические замечания не смешивать с несущими.
